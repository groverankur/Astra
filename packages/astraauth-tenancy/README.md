# Astra Mandal (`astraauth-tenancy`)

Astra Mandal is the multi-tenant isolation routing and dynamic tenancy middleware for the AstraAuth platform.

## Features
- Dynamic connection routing (multi-db schemas, distinct connection pools)
- Tenant Workspace configurations and metadata
- Dynamic tenancy middleware routing requests using headers/subdomains
